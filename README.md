# K-Means Compression Visualization

![example](imgs/example1.png)


## Requirements

- JDK 17+
- Kotlin 2.0+
- [raylib](https://www.raylib.com/) installed system-wide (`sudo pacman -S raylib`)


## Usage
```bash
./gradlew run
```

Press `SPACE` to run one iteration of K-Means.


